import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pro.thedifference.nublebus',
  appName: 'NubleBusApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
